#!/usr/bin/env python3
# Generated by tutor-handouts. PyTorch reference for {{Exercise Name}}.

from __future__ import annotations

import torch


def {{kernel_name}}({{HOST_PARAMS}}) -> torch.Tensor:
    {{PYTORCH_REFERENCE_BODY}}


if __name__ == "__main__":
    N = {{DEFAULT_N}}
    print(f"{{Exercise Name}} (PyTorch reference) — N={N}")
    {{PYTORCH_DEMO}}
