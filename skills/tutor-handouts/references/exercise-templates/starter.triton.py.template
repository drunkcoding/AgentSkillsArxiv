#!/usr/bin/env python3
# Generated by tutor-handouts. Triton starter for {{Exercise Name}}.
# Read the matching assignment PDF in Handouts/{{NN-topic}}/assignment-{{NN-slug}}.pdf.
#
# ECR-12: must import (`python -c "import ast; ast.parse(open('starter.triton.py').read())"` exits 0)
# ECR-13: must NOT solve
# ECR-14: ≥3 numbered FIXMEs
# ECR-15: each FIXME cites a vault concept note

from __future__ import annotations

import torch

try:
    import triton
    import triton.language as tl
except ImportError:
    raise ImportError("Triton is required. Install with `pip install triton`.")


@triton.jit
def {{kernel_name}}_kernel({{KERNEL_PARAMS}}, BLOCK_SIZE: tl.constexpr):
    pid = tl.program_id(0)
    # FIXME 1: {{FIXME_1_DESC}}  (see StudyVault/{{NN-topic}}/Concepts/{{concept-slug-1}}.md)
    # FIXME 2: {{FIXME_2_DESC}}  (see StudyVault/{{NN-topic}}/Concepts/{{concept-slug-2}}.md)
    # FIXME 3: {{FIXME_3_DESC}}  (see StudyVault/{{NN-topic}}/Concepts/{{concept-slug-3}}.md)


def {{kernel_name}}({{HOST_PARAMS}}) -> torch.Tensor:
    {{HOST_WRAPPER_BODY}}


if __name__ == "__main__":
    N = {{DEFAULT_N}}
    print(f"{{Exercise Name}} starter — N={N}")
    print("Fill in the FIXME markers, then re-run.")
