#!/usr/bin/env python3
# Generated by tutor-handouts. PyTorch starter for {{Exercise Name}}.
# Often the PyTorch version is one-liner-trivial; this file mainly demonstrates
# what the reference implementation looks like in eager mode and provides a
# correctness baseline for the CUDA and Triton starters to compare against.

from __future__ import annotations

import torch


def {{kernel_name}}({{HOST_PARAMS}}) -> torch.Tensor:
    # FIXME 1: {{FIXME_1_DESC}}  (see StudyVault/{{NN-topic}}/Concepts/{{concept-slug-1}}.md)
    raise NotImplementedError("Fill in the FIXMEs above.")


if __name__ == "__main__":
    N = {{DEFAULT_N}}
    print(f"{{Exercise Name}} (PyTorch) starter — N={N}")
    print("Fill in the FIXME markers, then re-run.")
